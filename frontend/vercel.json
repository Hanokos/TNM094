{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "devCommand": "npm start"  
      }
    }
  ],
  "env": {
    "NODE_ENV": "development"  
  }
}
